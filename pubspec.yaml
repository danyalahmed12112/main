name: esp32_smart_control
description: ESP32 Smart Control App
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_blue_plus: ^1.32.7
  http: ^1.2.0
  shared_preferences: ^2.2.2
  permission_handler: ^11.3.1

  cupertino_icons: ^1.0.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true